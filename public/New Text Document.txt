<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>BeatsEdge</title>
  <style>
    body {
      background: #0b0f1a;
      color: #fff;
      font-family: Arial, sans-serif;
      padding: 20px;
    }
    button {
      padding: 10px 16px;
      margin: 6px 0;
      font-size: 16px;
      cursor: pointer;
    }
    pre {
      background: #111;
      padding: 10px;
      max-height: 300px;
      overflow: auto;
    }
  </style>
</head>
<body>

  <h1>BeatsEdge API Test</h1>

  <button onclick="loadProps()">Load PrizePicks Props</button>
  <button onclick="loadEdges()">Build Edges</button>
  <button onclick="loadSlip()">Build 2-Leg Slip</button>

  <pre id="output">Click a button</pre>

  <script>
    const out = document.getElementById('output')

    async function loadProps() {
      const r = await fetch('/api/player-props')
      out.textContent = JSON.stringify(await r.json(), null, 2)
    }

    async function loadEdges() {
      const r = await fetch('/api/edges/today')
      out.textContent = JSON.stringify(await r.json(), null, 2)
    }

    async function loadSlip() {
      const r = await fetch('/api/prizepicks/slips?legs=2')
      out.textContent = JSON.stringify(await r.json(), null, 2)
    }
  </script>

</body>
</html>
